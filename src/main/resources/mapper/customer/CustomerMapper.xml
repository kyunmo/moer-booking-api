<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.moer.booking.domain.customer.repository.CustomerRepository">

    <!-- ResultMap -->
    <resultMap id="CustomerResultMap" type="io.moer.booking.domain.customer.Customer">
        <id property="id" column="id"/>
        <result property="businessId" column="business_id"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="visitCount" column="visit_count"/>
        <result property="totalSpent" column="total_spent"/>
        <result property="lastVisitDate" column="last_visit_date"/>
        <result property="tags" column="tags"
                typeHandler="io.moer.booking.common.mybatis.JsonTypeHandler"/>
        <result property="adminMemo" column="admin_memo"/>
        <result property="kakaoUserKey" column="kakao_user_key"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 생성 -->
    <insert id="save" parameterType="io.moer.booking.domain.customer.Customer"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customers (
            business_id, name, phone, email, visit_count, total_spent, last_visit_date,
            tags, admin_memo, kakao_user_key
        ) VALUES (
                     #{businessId}, #{name}, #{phone}, #{email},
                     COALESCE(#{visitCount}, 0), COALESCE(#{totalSpent}, 0), #{lastVisitDate},
                     #{tags, typeHandler=io.moer.booking.common.mybatis.JsonTypeHandler}::jsonb,
                     #{adminMemo}, #{kakaoUserKey}
                 )
    </insert>

    <!-- 단건 조회 -->
    <select id="findById" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE id = #{id}
    </select>

    <!-- Business의 Customer 목록 -->
    <select id="findByBusinessId" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
        ORDER BY visit_count DESC, created_at DESC
    </select>

    <!-- Business + 전화번호로 고객 조회 (중복 체크용) -->
    <select id="findByBusinessIdAndPhone" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND phone = #{phone}
    </select>

    <!-- 조건별 검색 (동적 쿼리) -->
    <select id="findByCondition" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
        <if test="name != null and name != ''">
            AND name LIKE '%' || #{name} || '%'
        </if>
        <if test="phone != null and phone != ''">
            AND phone LIKE '%' || #{phone} || '%'
        </if>
        <if test="tag != null and tag != ''">
            AND tags @> #{tag}::text::jsonb
        </if>
        <if test="minVisitCount != null">
            AND visit_count >= #{minVisitCount}
        </if>
        ORDER BY
        <choose>
            <when test="sortBy == 'total_spent'">
                total_spent
            </when>
            <when test="sortBy == 'last_visit_date'">
                last_visit_date
            </when>
            <otherwise>
                visit_count
            </otherwise>
        </choose>
        <choose>
            <when test="sortOrder == 'asc'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
    </select>

    <!-- VIP 고객 조회 (10회 이상) -->
    <select id="findVipCustomers" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND visit_count >= 10
        ORDER BY visit_count DESC
    </select>

    <!-- 신규 고객 조회 (1회) -->
    <select id="findNewCustomers" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND visit_count = 1
        ORDER BY created_at DESC
    </select>

    <!-- 단골 고객 조회 (3회 이상) -->
    <select id="findRegularCustomers" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND visit_count >= 3
        ORDER BY visit_count DESC
    </select>

    <!-- 수정 -->
    <update id="update">
        UPDATE customers
        SET
        <if test="name != null">name = #{name},</if>
        <if test="phone != null">phone = #{phone},</if>
        <if test="email != null">email = #{email},</if>
        <if test="tags != null">
            tags = #{tags, typeHandler=io.moer.booking.common.mybatis.JsonTypeHandler}::jsonb,
        </if>
        <if test="adminMemo != null">admin_memo = #{adminMemo},</if>
        <if test="kakaoUserKey != null">kakao_user_key = #{kakaoUserKey},</if>
        updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 방문 통계 업데이트 (예약 완료 시 호출) -->
    <update id="updateVisitStats">
        UPDATE customers
        SET
            visit_count = #{visitCount},
            total_spent = #{totalSpent},
            last_visit_date = #{lastVisitDate},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="delete">
        DELETE FROM customers
        WHERE id = #{id}
    </delete>

    <!-- 존재 확인 -->
    <select id="existsById" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM customers WHERE id = #{id}
        )
    </select>

    <!-- Business + 전화번호 중복 확인 -->
    <select id="existsByBusinessIdAndPhone" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM customers
            WHERE business_id = #{businessId}
              AND phone = #{phone}
        )
    </select>

    <!-- Business의 Customer 존재 확인 -->
    <select id="existsByBusinessIdAndId" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM customers
            WHERE business_id = #{businessId} AND id = #{id}
        )
    </select>

    <!-- Business의 Customer 개수 -->
    <select id="countByBusinessId" resultType="long">
        SELECT COUNT(*) FROM customers
        WHERE business_id = #{businessId}
    </select>

</mapper>
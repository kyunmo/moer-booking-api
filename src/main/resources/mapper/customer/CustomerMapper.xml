<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.moer.booking.domain.customer.repository.CustomerRepository">

    <!-- ResultMap -->
    <resultMap id="CustomerResultMap" type="io.moer.booking.domain.customer.Customer">
        <id property="id" column="id"/>
        <result property="businessId" column="business_id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="birthDate" column="birth_date"/>
        <result property="gender" column="gender"/>
        <result property="visitCount" column="visit_count"/>
        <result property="totalSpent" column="total_spent"/>
        <result property="lastVisitDate" column="last_visit_date"/>
        <result property="tags" column="tags"/>
        <result property="memo" column="memo"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 생성 -->
    <insert id="save" parameterType="io.moer.booking.domain.customer.Customer"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customers (
            business_id, user_id, name, phone, email, birth_date, gender,
            visit_count, total_spent, last_visit_date, tags, memo
        ) VALUES (
                     #{businessId}, #{userId}, #{name}, #{phone}, #{email}, #{birthDate}, #{gender},
                     #{visitCount}, #{totalSpent}, #{lastVisitDate}, #{tags}, #{memo}
                 )
    </insert>

    <!-- 단건 조회 -->
    <select id="findById" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE id = #{id}
    </select>

    <!-- Business의 전체 고객 조회 -->
    <select id="findByBusinessId" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
        ORDER BY created_at DESC
    </select>

    <!-- 전화번호로 고객 조회 -->
    <select id="findByBusinessIdAndPhone" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND phone = #{phone}
    </select>

    <!-- 조건별 고객 검색 -->
    <select id="findByCondition" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
        <if test="name != null and name != ''">
            AND name LIKE '%' || #{name} || '%'
        </if>
        <if test="phone != null and phone != ''">
            AND phone LIKE '%' || #{phone} || '%'
        </if>
        <if test="tag != null and tag != ''">
            AND tags LIKE '%' || #{tag} || '%'
        </if>
        <if test="minVisitCount != null">
            AND visit_count >= #{minVisitCount}
        </if>
        ORDER BY
        <choose>
            <when test="sortBy == 'name'">name</when>
            <when test="sortBy == 'total_spent'">total_spent</when>
            <when test="sortBy == 'last_visit_date'">last_visit_date</when>
            <otherwise>visit_count</otherwise>
        </choose>
        <choose>
            <when test="sortOrder == 'asc'">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>
    </select>

    <!-- VIP 고객 조회 -->
    <select id="findVipCustomers" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND visit_count >= 10
        ORDER BY visit_count DESC
    </select>

    <!-- 신규 고객 조회 -->
    <select id="findNewCustomers" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND visit_count = 1
        ORDER BY created_at DESC
    </select>

    <!-- 단골 고객 조회 -->
    <select id="findRegularCustomers" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE business_id = #{businessId}
          AND visit_count >= 3
        ORDER BY visit_count DESC
    </select>

    <!-- 수정 -->
    <update id="update">
        UPDATE customers
        SET name = #{name},
            phone = #{phone},
            email = #{email},
            birth_date = #{birthDate},
            gender = #{gender},
            tags = #{tags},
            memo = #{memo},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 방문 통계 업데이트 -->
    <update id="updateVisitStats">
        UPDATE customers
        SET visit_count = #{visitCount},
            total_spent = #{totalSpent},
            last_visit_date = #{lastVisitDate},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="delete">
        DELETE FROM customers
        WHERE id = #{id}
    </delete>

    <!-- 검증 -->
    <select id="existsById" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM customers WHERE id = #{id})
    </select>

    <select id="existsByBusinessIdAndPhone" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM customers
            WHERE business_id = #{businessId} AND phone = #{phone}
        )
    </select>

    <select id="existsByBusinessIdAndId" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM customers
            WHERE business_id = #{businessId} AND id = #{id}
        )
    </select>

    <select id="countByBusinessId" resultType="long">
        SELECT COUNT(*) FROM customers
        WHERE business_id = #{businessId}
    </select>

    <!-- 기간별 신규 고객 수 -->
    <select id="countByBusinessIdAndCreatedAtBetween" resultType="int">
        SELECT COUNT(*)
        FROM customers
        WHERE business_id = #{businessId}
          AND created_at BETWEEN #{startDateTime} AND #{endDateTime}
    </select>

    <!-- 최근 신규 고객 -->
    <select id="findRecentByBusinessId" resultType="io.moer.booking.domain.dashboard.dto.RecentCustomer">
        SELECT
            id,
            name,
            phone,
            visit_count AS visitCount,
            TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS') AS createdAt
        FROM customers
        WHERE business_id = #{businessId}
        ORDER BY created_at DESC
            LIMIT #{limit}
    </select>

    <!-- userId + businessId로 고객 조회 -->
    <select id="findByUserIdAndBusinessId" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE user_id = #{userId}
          AND business_id = #{businessId}
    </select>

    <!-- 고객의 user_id 업데이트 -->
    <update id="updateUserId">
        UPDATE customers
        SET user_id = #{userId},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{customerId}
    </update>

    <!-- ========================================
         대시보드 통계 - 액션 알림
         ======================================== -->

    <!-- 오늘 생일인 고객 수 -->
    <select id="countBirthdayCustomers" resultType="int">
        SELECT COUNT(*)
        FROM customers
        WHERE business_id = #{businessId}
          AND birth_date IS NOT NULL
          AND EXTRACT(MONTH FROM birth_date) = #{month}
          AND EXTRACT(DAY FROM birth_date) = #{day}
    </select>

    <!-- 재방문 유도 대상 고객 수 (N개월 이상 미방문) -->
    <select id="countInactiveCustomers" resultType="int">
        SELECT COUNT(*)
        FROM customers
        WHERE business_id = #{businessId}
          AND last_visit_date IS NOT NULL
          AND last_visit_date <![CDATA[<]]> CURRENT_DATE - INTERVAL '${monthsAgo} months'
          AND visit_count > 0
    </select>

    <!-- ========================================
         대시보드 통계 - 고객 세그먼트
         ======================================== -->

    <!-- 방문 횟수 범위별 고객 수 -->
    <select id="countByBusinessIdAndVisitCountBetween" resultType="int">
        SELECT COUNT(*)
        FROM customers
        WHERE business_id = #{businessId}
          AND visit_count BETWEEN #{minVisits} AND #{maxVisits}
    </select>

    <!-- 이탈 고객 수 (N개월 이상 미방문) -->
    <select id="countInactiveCustomersByLastVisit" resultType="int">
        SELECT COUNT(*)
        FROM customers
        WHERE business_id = #{businessId}
          AND last_visit_date IS NOT NULL
          AND last_visit_date <![CDATA[<]]> CURRENT_DATE - INTERVAL '${monthsAgo} months'
          AND visit_count > 0
    </select>

    <!-- 재방문 고객 수 (2회 이상) -->
    <select id="countReturningCustomers" resultType="int">
        SELECT COUNT(*)
        FROM customers
        WHERE business_id = #{businessId}
          AND visit_count >= 2
    </select>

    <!-- ========================================
         대시보드 통계 - 평균 지표
         ======================================== -->

    <!-- 평균 방문 횟수 -->
    <select id="getAverageVisitCount" resultType="Double">
        SELECT COALESCE(AVG(visit_count), 0.0)
        FROM customers
        WHERE business_id = #{businessId}
          AND visit_count > 0
    </select>

    <!-- 평균 고객 생애 가치 (LTV) -->
    <select id="getAverageCustomerLifetimeValue" resultType="Integer">
        SELECT COALESCE(AVG(total_spent), 0)::integer
        FROM customers
        WHERE business_id = #{businessId}
          AND visit_count > 0
    </select>

    <!-- ========================================
         Public 예약 조회 (이름+전화번호)
         ======================================== -->

    <!-- 이름+전화번호가 일치하는 모든 고객 조회 (businessId 무관) -->
    <select id="findAllByNameAndPhone" resultMap="CustomerResultMap">
        SELECT * FROM customers
        WHERE name = #{name}
          AND phone = #{phone}
    </select>
</mapper>
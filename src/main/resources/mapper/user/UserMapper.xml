<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.moer.booking.domain.user.repository.UserRepository">

    <!-- Result Map -->
    <resultMap id="UserResultMap" type="io.moer.booking.domain.user.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="staffId" column="staff_id"/>
        <result property="businessId" column="business_id"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="trialStartedAt" column="trial_started_at"/>
        <result property="trialExpiresAt" column="trial_expires_at"/>
        <result property="isPremium" column="is_premium"/>
        <result property="marketingAgree" column="marketing_agree"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- Base Column List -->
    <sql id="baseColumns">
        id, email, password, name, phone, profile_image_url, role, status,
        staff_id, business_id, email_verified,
        trial_started_at, trial_expires_at, is_premium, marketing_agree,
        last_login_at, created_at, updated_at
    </sql>

    <!-- 전체 조회 -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/>
        FROM users
        ORDER BY created_at DESC
    </select>

    <!-- ID로 조회 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 이메일로 조회 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/>
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 이메일 존재 여부 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM users WHERE email = #{email}
        )
    </select>

    <!-- 저장 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            email, password, name, phone, profile_image_url, role, status,
            staff_id, business_id, email_verified,
            trial_started_at, trial_expires_at, is_premium, marketing_agree
        ) VALUES (
            #{email}, #{password}, #{name}, #{phone}, #{profileImageUrl},
            #{role}, #{status},
            #{staffId}, #{businessId}, #{emailVerified},
            #{trialStartedAt}, #{trialExpiresAt}, #{isPremium}, #{marketingAgree}
        )
    </insert>

    <!-- 수정 -->
    <update id="update">
        UPDATE users
        SET name = #{name},
            phone = #{phone},
            staff_id = #{staffId},
            business_id = #{businessId},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 상태 변경 -->
    <update id="updateStatus">
        UPDATE users
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 마지막 로그인 시간 업데이트 -->
    <update id="updateLastLoginAt">
        UPDATE users
        SET last_login_at = #{lastLoginAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 비밀번호 업데이트 -->
    <update id="updatePassword">
        UPDATE users
        SET password = #{password},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 프로필 수정 (이름, 전화번호) -->
    <update id="updateProfile">
        UPDATE users
        SET name = #{name},
            phone = #{phone},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 프로필 이미지 URL 업데이트 -->
    <update id="updateProfileImageUrl">
        UPDATE users
        SET profile_image_url = #{profileImageUrl},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 검색 (페이징 포함) -->
    <select id="search" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/>
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                name LIKE '%' || #{keyword} || '%'
                OR email LIKE '%' || #{keyword} || '%'
                )
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="businessId != null">
                AND business_id = #{businessId}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 검색 결과 총 개수 -->
    <select id="countSearch" resultType="long">
        SELECT COUNT(*)
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                name LIKE '%' || #{keyword} || '%'
                OR email LIKE '%' || #{keyword} || '%'
                )
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="businessId != null">
                AND business_id = #{businessId}
            </if>
        </where>
    </select>

    <update id="updateBusinessId">
        UPDATE users
        SET business_id = #{businessId},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{userId}
    </update>

    <!-- 고객 프로필 수정 (name, phone, marketing_agree) -->
    <update id="updateCustomerProfile">
        UPDATE users
        SET name = #{name},
            phone = #{phone},
            marketing_agree = #{marketingAgree},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 고객(user_id)별 예약 수 -->
    <select id="countReservationsByUserId" resultType="int">
        SELECT COUNT(*)
        FROM reservations
        WHERE user_id = #{userId}
    </select>

    <!-- 고객(user_id)별 리뷰 수 -->
    <select id="countReviewsByUserId" resultType="int">
        SELECT COUNT(*)
        FROM reviews r
        INNER JOIN reservations res ON r.reservation_id = res.id
        WHERE res.user_id = #{userId}
          AND r.status = 'ACTIVE'
    </select>

    <!-- ========================================
         SuperAdmin 통계 쿼리
         ======================================== -->

    <!-- 전체 사용자 수 -->
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <!-- 역할별 사용자 수 -->
    <select id="countByRole" resultType="long">
        SELECT COUNT(*) FROM users
        WHERE role = #{role}::varchar
    </select>

    <!-- 특정 월에 생성된 사용자 수 -->
    <select id="countCreatedInMonth" resultType="long">
        SELECT COUNT(*) FROM users
        WHERE DATE_TRUNC('month', created_at) = DATE_TRUNC('month', #{date}::date)
    </select>

    <!-- ========================================
         SuperAdmin 사용자 관리
         ======================================== -->

    <!-- 사용자 삭제 -->
    <delete id="delete">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
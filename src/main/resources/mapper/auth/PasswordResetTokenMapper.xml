<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.moer.booking.domain.auth.repository.PasswordResetTokenRepository">

    <!-- Result Map -->
    <resultMap id="PasswordResetTokenResultMap" type="io.moer.booking.domain.auth.PasswordResetToken">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="token" column="token"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="used" column="used"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 토큰 저장 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO password_reset_tokens (
            user_id, token, expires_at, used
        ) VALUES (
            #{userId}, #{token}, #{expiresAt}, #{used}
        )
    </insert>

    <!-- 토큰으로 조회 -->
    <select id="findByToken" resultMap="PasswordResetTokenResultMap">
        SELECT id, user_id, token, expires_at, used, created_at
        FROM password_reset_tokens
        WHERE token = #{token}
    </select>

    <!-- 사용자 ID로 미사용 토큰 삭제 -->
    <delete id="deleteUnusedByUserId">
        DELETE FROM password_reset_tokens
        WHERE user_id = #{userId}
          AND used = 'N'
    </delete>

    <!-- 토큰을 사용 완료로 표시 -->
    <update id="markAsUsed">
        UPDATE password_reset_tokens
        SET used = 'Y'
        WHERE token = #{token}
    </update>

    <!-- 만료된 토큰 삭제 (배치 작업용) -->
    <delete id="deleteExpired">
        DELETE FROM password_reset_tokens
        WHERE expires_at &lt; CURRENT_TIMESTAMP
    </delete>

</mapper>

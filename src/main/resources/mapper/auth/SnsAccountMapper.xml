<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.moer.booking.domain.auth.repository.SnsAccountRepository">

    <!-- Result Map -->
    <resultMap id="SnsAccountResultMap" type="io.moer.booking.domain.auth.SnsAccount">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="provider" column="provider"/>
        <result property="providerUserId" column="provider_user_id"/>
        <result property="email" column="email"/>
        <result property="name" column="name"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- SNS 계정 저장 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sns_accounts (
            user_id, provider, provider_user_id, email, name, profile_image_url
        ) VALUES (
            #{userId},
            #{provider}::VARCHAR,
            #{providerUserId},
            #{email},
            #{name},
            #{profileImageUrl}
        )
    </insert>

    <!-- 제공자와 제공자 사용자 ID로 조회 -->
    <select id="findByProviderAndProviderUserId" resultMap="SnsAccountResultMap">
        SELECT id, user_id, provider, provider_user_id, email, name, profile_image_url, created_at, updated_at
        FROM sns_accounts
        WHERE provider = #{provider}::VARCHAR
          AND provider_user_id = #{providerUserId}
    </select>

    <!-- 사용자 ID로 조회 -->
    <select id="findByUserId" resultMap="SnsAccountResultMap">
        SELECT id, user_id, provider, provider_user_id, email, name, profile_image_url, created_at, updated_at
        FROM sns_accounts
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 이메일로 조회 -->
    <select id="findByEmail" resultMap="SnsAccountResultMap">
        SELECT id, user_id, provider, provider_user_id, email, name, profile_image_url, created_at, updated_at
        FROM sns_accounts
        WHERE email = #{email}
        ORDER BY created_at DESC
    </select>

    <!-- SNS 계정 삭제 -->
    <delete id="delete">
        DELETE FROM sns_accounts
        WHERE id = #{id}
    </delete>

    <!-- 사용자 ID로 모든 SNS 계정 삭제 -->
    <delete id="deleteByUserId">
        DELETE FROM sns_accounts
        WHERE user_id = #{userId}
    </delete>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.moer.booking.domain.notificationlog.repository.NotificationLogRepository">

    <resultMap id="NotificationLogResultMap" type="io.moer.booking.domain.notificationlog.NotificationLog">
        <id property="id" column="id"/>
        <result property="businessId" column="business_id"/>
        <result property="reservationId" column="reservation_id"/>
        <result property="channel" column="channel"/>
        <result property="templateType" column="template_type"/>
        <result property="recipientPhone" column="recipient_phone"/>
        <result property="recipientName" column="recipient_name"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="errorMessage" column="error_message"/>
        <result property="sentAt" column="sent_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification_logs (
            business_id, reservation_id, channel, template_type,
            recipient_phone, recipient_name, title, content,
            status, error_message, sent_at
        ) VALUES (
            #{businessId}, #{reservationId}, #{channel}, #{templateType},
            #{recipientPhone}, #{recipientName}, #{title}, #{content},
            #{status}, #{errorMessage}, #{sentAt}
        )
    </insert>

    <select id="findById" resultMap="NotificationLogResultMap">
        SELECT * FROM notification_logs WHERE id = #{id}
    </select>

    <select id="findByCondition" resultMap="NotificationLogResultMap">
        SELECT * FROM notification_logs
        <where>
            <if test="businessId != null">
                AND business_id = #{businessId}
            </if>
            <if test="channel != null">
                AND channel = #{channel}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="templateType != null">
                AND template_type = #{templateType}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM notification_logs
        <where>
            <if test="businessId != null">
                AND business_id = #{businessId}
            </if>
            <if test="channel != null">
                AND channel = #{channel}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="templateType != null">
                AND template_type = #{templateType}
            </if>
        </where>
    </select>

    <update id="updateStatus">
        UPDATE notification_logs
        SET status = #{status},
            error_message = #{errorMessage},
            sent_at = CASE WHEN #{status} = 'SENT' THEN CURRENT_TIMESTAMP ELSE sent_at END
        WHERE id = #{id}
    </update>

</mapper>

# 작업 이력 — 2026-05-12

## 개요
- **세션 시작**: 2026-05-12
- **작업 영역**: 서비스 런칭 전 보안 점검 + P0 항목 구현
- **연관 문서**: [docs/security/](../security/)

---

## 1. 전체 보안 감사 (오전)

5개 영역 병렬 분석 → 30건 발견 (Critical 7, High 11, Medium 8, Low 4)

작성 문서:
- `docs/security/audit-2026-05-12.md` — 감사 보고서
- `docs/security/improvement-plan.md` — P0~P3 개선 로드맵
- `docs/security/launch-checklist.md` — 배포 전 체크리스트
- `docs/security/README.md` — 인덱스

---

## 2. P0 항목 구현

상태 표기: ✅ 완료 / ⚠️ 사용자 액션 필요

### ✅ P0-1. 시크릿 환경변수화 (코드/설정 분)

**변경 파일**:
- `src/main/resources/application.yml`
  - DB password, JWT secret, OAuth client-secret 모두 기본값 제거 (`${VAR:}` 빈 fallback)
  - `spring.profiles.active` 기본값 `local` 추가
  - `UPLOAD_DIR` 절대경로 → 상대경로 `./uploads`
- `src/main/java/io/moer/booking/common/security/JwtProperties.java`
  - 하드코딩 기본값 제거 (`private String secret;` 로 변경)
- `src/main/java/io/moer/booking/common/config/SecretsValidator.java` **(신규)**
  - `@PostConstruct` 로 부팅 시 필수 환경변수 검증, 누락 시 `IllegalStateException` Fail Fast
  - JWT 시크릿 32바이트(256bit) 이상 길이 검증
  - prod 프로필에서 추가 검증 (MAIL_PASSWORD 등)
- `docker-compose.yml`
  - DB 비밀번호 평문 제거, `${POSTGRES_PASSWORD:?...}` 로 부재 시 실패
  - healthcheck 사용자 `chatbot` → `${POSTGRES_USER:-moer}` 로 정정
- `.gitignore`
  - `application-local.yml`, `application-prod.yml`, `application-dev.yml` 명시적 제외
  - `.env`, `.env.local`, `.env.prod` 제외
  - `/uploads/`, `uploads/` 제외
- `.env.example` **(신규)** — 환경변수 템플릿
- `src/main/resources/application-local.yml.example` **(신규)** — 로컬 프로필 템플릿
- `src/main/resources/application-prod.yml.example` **(신규)** — 운영 프로필 템플릿 (Swagger 비활성, DEBUG 로그 차단)
- `docs/security/ENV-SETUP.md` **(신규)** — 환경변수 설정 가이드

### ✅ P0-2. MyBatis SQL Injection 제거

**변경 파일**:
- `src/main/resources/mapper/business/BusinessSettingsMapper.xml:162-180`
  - `SET ${column} = #{value}` → `<choose><when>` 화이트리스트 분기 (XML 자체에서 컬럼 화이트리스트 강제)
  - 화이트리스트 외 입력 시 `invalid_column_blocked_by_whitelist`로 의도적 SQL 오류 발생
- `src/main/resources/mapper/customer/CustomerMapper.xml:217-227`, `239-249`
  - `INTERVAL '${monthsAgo} months'` → `MAKE_INTERVAL(months => #{monthsAgo})` 로 PostgreSQL 안전 함수 + 파라미터 바인딩

**검증**: `grep -rn '\${' src/main/resources/mapper/` → 0건

### ✅ P0-3. UserController 권한 검증 도입

**변경 파일**:
- `src/main/java/io/moer/booking/domain/user/controller/UserController.java`
  - `@PreAuthorize` 어노테이션을 모든 메서드에 추가:
    - `GET /api/users` — `hasAnyRole('ADMIN','SUPER_ADMIN')`
    - `GET /api/users/{userId}` — 본인 or 관리자 (`#userId == principal.userId or hasAnyRole(...)`)
    - `GET /api/users/email/{email}` — 관리자 전용
    - `PATCH /api/users/{userId}` — 본인 or 관리자
    - `PATCH /api/users/{userId}/status` — 관리자 전용
    - `GET /api/users/search` — 관리자 전용
  - `GET /api/users/check-email` 은 회원가입 플로우용으로 비인증 유지 (Rate Limit는 P1-3에서)

**기반**: Spring Security `@EnableMethodSecurity` 이미 활성화됨 (SecurityConfig.java:34). CustomUserDetails에 `getUserId()` 존재해 SpEL `principal.userId` 사용 가능.

### ✅ P0-4. Path Traversal 차단

**변경 파일**:
- `src/main/java/io/moer/booking/common/storage/LocalFileStorageService.java`
  - `uploadRoot` 필드 추가 (정규화된 절대 경로 캐시)
  - `store()`: subDir 화이트리스트 검증(`[a-zA-Z0-9_\-/]+`), 정규화 후 `startsWith(uploadRoot)` 검증 2회
  - `delete()`: 정규화 후 `startsWith(uploadRoot)` 검증, 위반 시 조용히 거부(공격자 정보 노출 회피)
  - 시도 감지 시 `log.warn` 으로 기록

### ✅ P0-5. HTTP 보안 헤더 적용

**변경 파일**:
- `src/main/java/io/moer/booking/common/config/SecurityConfig.java`
  - `Environment` 주입 + `isProdProfile()` 헬퍼
  - `.headers(...)` 체인 추가:
    - `Strict-Transport-Security: max-age=31536000; includeSubDomains`
    - `X-Frame-Options: DENY`
    - `X-Content-Type-Options: nosniff`
    - `Referrer-Policy: same-origin`
    - `Content-Security-Policy`: prod는 strict (`default-src 'self'; frame-ancestors 'none'`), 개발은 Swagger 호환 완화
  - CORS `allowedHeaders` 와일드카드 제거 → `Authorization, Content-Type, Accept, X-Requested-With, X-Customer-Token` 화이트리스트

### ✅ P0-6. Swagger 프로덕션 비활성화

**변경 파일**:
- `src/main/java/io/moer/booking/common/config/SecurityConfig.java`
  - prod 프로필일 때 `/v3/api-docs/**`, `/swagger-ui/**` `denyAll()`
  - dev/local 프로필일 때만 `permitAll()`
- `src/main/java/io/moer/booking/common/config/SwaggerConfig.java`
  - API 설명에서 테스트 계정 정보 제거 (`admin@moer.io / password123` 등)
- `src/main/resources/application-prod.yml.example`
  - `springdoc.api-docs.enabled: false`, `springdoc.swagger-ui.enabled: false`

### ✅ 빌드 검증

- `./gradlew compileJava` → BUILD SUCCESSFUL (1m 15s)
- 컴파일 오류 없음

---

## 2.5 Profile 기반 설정 + 즉시 실행 가능 상태 구성

P0~P3 종료 후 사용자 요청으로 Spring Profile 분리 + 노출 시크릿 마킹 후 부팅 가능 상태로 정리.

**신규 파일** (모두 `.gitignore` 등록되어 커밋 안 됨):
- `src/main/resources/application-local.yml` — 로컬 개발 프로필
  - 노출됐던 시크릿 그대로 포함, 각 항목에 `[LEAKED-SECRET / TODO-ROTATE]` 코멘트로 마킹
  - DEBUG 로깅 활성화
  - localhost CORS / OAuth redirect
- `src/main/resources/application-prod.yml` — 운영 배포 프로필
  - 모든 시크릿 환경변수로만 주입 (기본값 없음)
  - Swagger/API Docs 비활성화
  - WARN 로깅
  - Hikari 풀 사이즈 운영 기준 (20/10)
- `.env` — docker-compose 자동 로드용 (POSTGRES_*)

**RateLimitFilter 수정**:
- Spring Boot 4 의 starter 구조에서 `ObjectMapper` bean 이 자동 등록되지 않는 이슈 발견
- 생성자 주입 → 자체 `new ObjectMapper()` 인스턴스로 변경 (Spring 컨테이너 의존성 제거)

**부팅 검증**:
```
The following 1 profile is active: "local"
[SecretsValidator] 시크릿 검증 완료 (profile=local)
Filter 'jwtAuthenticationFilter' configured for use
Filter 'rateLimitFilter' configured for use
Filter 'pageSizeCapFilter' configured for use
Tomcat started on port 8080
Started MoerBookingApplication in 25.317 seconds
```

`./gradlew bootRun` 실행 성공 (PostgreSQL 컨테이너 실행 중 가정). SecretsValidator 통과, 3개 보안 필터 정상 등록, Tomcat 정상 부팅.

---

## 3. 사용자 후속 액션 (코드 변경으로 처리 불가)

P0를 완전히 마무리하려면 다음 외부 작업이 필요합니다. **현재는 코드/설정만 안전화된 상태이고, 과거 노출된 시크릿은 여전히 git 이력과 외부 서비스에 유효합니다.**

### ⚠️ A. 외부 콘솔에서 시크릿 회전 (반드시 필요)

각 콘솔에 접속하여 노출된 시크릿을 폐기 + 재발급:

1. **PostgreSQL**: `moer` 사용자 비밀번호 변경 (구 `moer2026@` 폐기)
2. **Google Cloud Console** → OAuth 2.0 Client → `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` 폐기 + 재발급
3. **Naver Developers** → 애플리케이션 → `JcUZkcUPhe` 폐기 + 재발급
4. **Kakao Developers** → 애플리케이션 → `8zOJXees3y4v1Zz2vckIfmhjCVYRNVcU` 폐기 + 재발급
5. **네이버웍스 SMTP**: `kkm@moer.io` 비밀번호 변경 (구 `U3SXTOXHzkPX` 폐기)
6. **JWT 시크릿** 신규 생성:
   ```bash
   openssl rand -base64 48
   # 또는 PowerShell:
   # $b = New-Object byte[] 48; [Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b); [Convert]::ToBase64String($b)
   ```

### ⚠️ B. 로컬 환경 설정

작업 후 다음 중 하나로 로컬 부팅 환경 구성:

- `cp .env.example .env` 후 값 입력 (IntelliJ EnvFile 플러그인 사용 권장)
- 또는 `cp src/main/resources/application-local.yml.example src/main/resources/application-local.yml` 후 값 입력

자세한 가이드: `docs/security/ENV-SETUP.md`

### ⚠️ C. git 이력 정리 (선택, 그러나 강력 권고)

현재 `application.yml`, `docker-compose.yml`의 과거 커밋에 시크릿이 평문으로 남아있습니다. 이력에서 제거하려면 **history rewriting 작업**이 필요하며, 이는 **모든 협업자가 재클론**해야 하는 파괴적 작업입니다. 진행 시:

```bash
# BFG Repo-Cleaner 사용 예
java -jar bfg.jar --replace-text passwords.txt   # passwords.txt 에 노출된 값 나열
git reflog expire --expire=now --all && git gc --prune=now --aggressive
git push --force-with-lease
```

**또는** 위 A단계로 시크릿이 모두 무효화되었다면 이력 정리는 생략 가능 (노출된 값이 더는 유효하지 않으므로).

---

## 4. P1 항목 구현

### ✅ P1-2 / P1-8: 통합 완료
- P1-2 (JWT 시크릿 강제 환경변수) → P0-1 `SecretsValidator` 에서 이미 처리됨 (32바이트 검증 포함)
- P1-8 (CORS allowedHeaders 화이트리스트) → P0-5 와 함께 처리됨 (`Authorization, Content-Type, Accept, X-Requested-With, X-Customer-Token`)

### ✅ P1-7: PII 로그 마스킹

**신규 파일**:
- `src/main/java/io/moer/booking/common/util/MaskingUtils.java` — `maskPhone/maskEmail/maskName/maskToken`

**변경 파일** (PII 평문 로그 → 마스킹):
- `InquiryService` — name/email 마스킹
- `CustomerService` — name/phone 마스킹 (3개 로그 라인)
- `PublicBookingService` — name/phone 마스킹 (lookupReservations, 취소)
- `EmailService` — 모든 to= 로그 마스킹, 토큰 마스킹 일원화
- `AuthService` — login/register/passwordReset/deleteAccount 의 email 마스킹

### ✅ P1-6: LIKE 와일드카드 이스케이프

**신규 파일**:
- `src/main/java/io/moer/booking/common/util/SqlEscapeUtils.java` — `escapeLike()`

**변경 mapper** (사용자 입력 LIKE 검색에 `<bind>` + `ESCAPE '\'` 적용):
- `BusinessMapper.xml` — 5개 검색 쿼리 (findAll, countAll, searchPublic, countSearchPublic, searchNearby/countSearchNearby)
- `CustomerMapper.xml` — name/phone/tag 검색 + 다중 tag (foreach 내 inline REPLACE)
- `UserMapper.xml` — search/countSearch
- `StaffMapper.xml` — name/specialty 검색
- `HelpArticleMapper.xml` — find/countByCondition
- `CouponMapper.xml` — find/countByCondition

### ✅ P1-9: originalFilename / PG 거래번호 보호

**신규 파일**:
- `src/main/java/io/moer/booking/common/util/FilenameUtils.java` — `sanitize()` (path traversal, 제어문자, 길이 255 제한, 한글/ASCII 안전 문자만 허용)

**변경 파일**:
- `ReviewService` — originalFilename 저장 전 `FilenameUtils.sanitize()`, 로그도 정화된 값 사용
- `CustomerReviewController` — multipart 업로드 시 정화
- `PaymentResponse` — `pgTransactionIdMasked` 필드 추가, `MaskingUtils.maskToken()` 적용. 기존 `pgTransactionId` 는 OWNER 사용 위해 유지하나 UI 에서는 마스킹 필드 우선 사용 권장.

### ✅ P1-5: XSS / 입력 검증 강화

**의존성 추가** (`build.gradle.kts`):
- `com.googlecode.owasp-java-html-sanitizer:owasp-java-html-sanitizer:20240325.1`

**신규 파일**:
- `src/main/java/io/moer/booking/common/util/HtmlSanitizer.java` — `plainText()` (태그 전제거), `safeRichText()` (안전 서식만)

**변경 파일** (사용자 입력 content 저장 전 `HtmlSanitizer.plainText()` 적용):
- `ReviewService` — 리뷰 content 2곳 (createReview, createReviewByCustomer)
- `InquiryService` — name, content
- `CustomerService` — 고객 노트 content (create, update)

### ✅ P1-4: Business / Dashboard 접근 통제 일관성

**변경 파일**:
- `User.java` — `requireAccessBusiness(businessId)`, `requireAccessStaff(staffId)` 추가 (위반 시 `AccessDeniedException`)
- `CustomUserDetails.java` — 동일 enforce 메서드 위임
- **broken caller 일괄 교체** (반환값 무시 케이스):
  - `BusinessController` (2곳)
  - `DashboardController` (3곳)
  - `CustomerController` (10곳)
  - `StatisticsController` (5곳)
  - `NotificationLogController` (2곳)
  - `StaffScheduleController` (3곳)

기존에 `if (!canAccessBusiness(...))` 패턴으로 올바르게 검증하던 `BusinessService`, `BusinessSlugController`, `BusinessNotificationController` 는 그대로 유지.

### ✅ P1-3: 인증/공개 엔드포인트 Rate Limiting

**의존성 추가**:
- `com.bucket4j:bucket4j_jdk17-core:8.14.0`

**신규 파일**:
- `src/main/java/io/moer/booking/common/ratelimit/RateLimiterService.java` — 인메모리 토큰 버킷, `RateLimitPolicy` (LOGIN: 5/15분, FORGOT_PASSWORD: 3/시간, CHECK_EMAIL: 10/분, REGISTER: 5/시간, PUBLIC_SEARCH: 60/분)
- `src/main/java/io/moer/booking/common/ratelimit/RateLimitFilter.java` — `OncePerRequestFilter`, IP 추출(X-Forwarded-For 우선), 429 응답 + `Retry-After` 헤더

**적용 엔드포인트**:
- `POST /api/auth/login`, `POST /api/auth/register`, `POST /api/auth/forgot-password`, `POST /api/auth/reset-password`
- `GET /api/users/check-email`, `POST /api/users`
- `GET /api/public/businesses/**`, `GET /api/public/help/**`

**계정 기반 로그인 잠금** (`AuthService.login()`):
- 동일 이메일에 대해 IP 기반과 별도로 15분 5회 실패 시 잠금
- 로그인 성공 시 카운터 리셋

### ✅ P1-1: Refresh Token 해시 저장 + Rotation

**스키마 변경** (`src/main/resources/db/schema.sql`):
- `refresh_tokens.token` (VARCHAR(500)) → `refresh_tokens.token_hash` (VARCHAR(255))
- 토큰 unique index 제거 (조회는 user_id 기준)

**Entity / Repository / Mapper**:
- `RefreshToken.java` — `token` → `tokenHash` 필드
- `RefreshTokenRepository.java` — `findByToken/deleteByToken` 제거 (조회는 user_id 기준)
- `RefreshTokenMapper.xml` — token_hash 컬럼 매핑

**AuthService 로직 변경**:
- `login()`, `register()`: 저장 시 `passwordEncoder.encode(refreshToken)` 으로 해시 저장
- `refreshAccessToken()`:
  1. raw 토큰 JWT 서명/만료 검증
  2. JWT claims 의 userId 로 저장된 해시 조회
  3. `passwordEncoder.matches(rawToken, storedHash)` — **불일치 시 재사용 의심 → 전체 세션 무효화 + 로그 경고**
  4. 매칭 시: 새 access+refresh 발급, 이전 해시 폐기 + 새 해시 저장(Rotation)
- `OAuth2AuthenticationSuccessHandler` — OAuth2 성공 시에도 해시 저장으로 통일

**영향**: 기존 발급된 refresh token 은 모두 무효화됨 (사용자 재로그인 필요). 사전 런칭 단계라 수용 가능.

### ✅ 빌드 검증
- `./gradlew compileJava` → **BUILD SUCCESSFUL** (2m 57s)
- Lombok `@Builder` 경고 1건 (`LoginResponse.tokenType` 초기값 — P1 작업과 무관, 기존 코드)

---

## 5. P2 항목 구현

### ✅ P2-4 / P2-6 / P2-8: 통합 완료
- P2-4 (미등록 이메일 익명화) → P1-7 에서 `MaskingUtils.maskEmail(email)` 적용 완료
- P2-6 (docker-compose healthcheck) → P0-1 에서 `${POSTGRES_USER:-moer}` 로 정정 완료
- P2-8 (UPLOAD_DIR OS 중립화) → P0-1 에서 `./uploads` 상대경로로 변경, prod 프로필 example 에 `/var/lib/moer/uploads` 권장

### ✅ P2-1: BCrypt strength 12

**변경 파일**:
- `src/main/java/io/moer/booking/common/config/PasswordEncoderConfig.java` — `new BCryptPasswordEncoder(12)` 명시
- 부수효과: P1-1 의 Refresh Token 해시 강도도 함께 상향

### ✅ P2-2: Pagination 상한 글로벌 강제

36개 파일에 `size` 파라미터가 산재 → 개별 수정 대신 Servlet Filter 로 글로벌 캡 적용.

**신규 파일**:
- `src/main/java/io/moer/booking/common/pagination/PageSizeCapFilter.java`
  - `MAX_PAGE_SIZE = 100`
  - 모든 요청의 `size` query param 을 `HttpServletRequestWrapper` 로 가로채 100 으로 캡
  - `Order(HIGHEST_PRECEDENCE + 20)` 으로 RateLimitFilter(+10) 다음에 동작
  - 음수/0/비숫자는 컨트롤러 단 기본값 처리에 위임

### ✅ P2-3: 환경별 로깅 분리

**변경 파일**:
- `src/main/resources/application.yml` — 기본 로깅을 모두 INFO 로 변경, 잘못된 패키지 이름 `com.moer.booking` → `io.moer.booking` 정정. DEBUG 활성화 제거.
- `src/main/resources/application-local.yml.example` — 로컬 개발자가 DEBUG 활성화하도록 템플릿 제공
- `src/main/resources/application-prod.yml.example` — prod 에서 WARN 강제 (이미 P0-6 에서 작성됨)

### ✅ P2-5: Actuator 명시적 보안 설정

현재 actuator 의존성이 없어 노출 없음. 향후 추가될 경우 안전 기본값을 사전 적용.

**변경 파일**:
- `src/main/resources/application.yml` — `management.endpoints.web.exposure.include: health,info` + `health.show-details: when-authorized` + `info.env.enabled: false`
- `src/main/java/io/moer/booking/common/config/SecurityConfig.java`
  - `/actuator/health`, `/actuator/info` → `permitAll()`
  - `/actuator/**` 나머지 → `hasAnyRole("ADMIN", "SUPER_ADMIN")`

### ✅ P2-7: 의존성 버전 명시 + 취약점 스캔

**변경 파일**:
- `build.gradle.kts`
  - `org.owasp.dependencycheck` 11.1.1 플러그인 추가
  - jackson-databind, postgresql 에 BOM 사용 명시 주석 추가
  - `dependencyCheck { failBuildOnCVSS = 7.0f; formats = HTML+JSON; skipConfigurations = test }`
- 실행: `./gradlew dependencyCheckAnalyze` (CI 또는 수동 주기적 실행). 빌드 자동 실행 X (NVD 다운로드 부하)

### ✅ 빌드 검증
- `./gradlew compileJava` → **BUILD SUCCESSFUL** (4m 57s, 캐시 미사용)
- 경고는 P1 와 동일 (`LoginResponse.tokenType` Lombok 경고, 기존 코드)

---

## 6. P3 항목 구현

### ✅ P3-1: DB 스키마 ENUM CHECK 제약

**변경 파일**:
- `src/main/resources/db/schema.sql` — `users` 테이블의 role/status/Y-N 컬럼에 CHECK 제약 추가 + `password_change_required` 컬럼 신설
- `src/main/resources/db/migration/V20260512__security_enum_check_constraints.sql` (신규) — 기존 환경 마이그레이션용 ALTER TABLE 스크립트 (businesses, reservations, customers, audit_logs, business_coupons, subscription_coupons, payments, inquiries, notification_logs, broadcasts)

### ✅ P3-2: Validation 오류 응답 정제

**변경 파일**:
- `src/main/java/io/moer/booking/common/exception/GlobalExceptionHandler.java`
  - `Environment` 주입 + `isProdProfile()` 추가
  - `buildValidationDetails()` 헬퍼: prod 에서는 필드명/내부 메시지 미노출, "입력값을 확인해주세요" 단일 메시지. dev/local 은 기존 디테일 유지.

### ✅ P3-3: CORS 정의 단일화

**변경 파일**:
- `src/main/java/io/moer/booking/common/config/WebConfig.java` — `addCorsMappings` 제거 (SecurityConfig 단일 소스)
- `SecurityConfig#corsConfigurationSource()` 에서만 정의 (P0-5 에서 적용된 화이트리스트 헤더 유지)

### ✅ P3-4: 초기 비밀번호 강제 변경 + 문서 비밀번호 제거

**변경 파일**:
- `src/main/resources/db/schema.sql` — `users.password_change_required CHAR(1)` 컬럼 추가 (P3-1과 함께)
- `src/main/java/io/moer/booking/domain/user/User.java` — `passwordChangeRequired` 필드 + `isPasswordChangeRequired()` 헬퍼
- `src/main/resources/mapper/user/UserMapper.xml` — resultMap, baseColumns, INSERT 에 컬럼 반영. `updatePassword` 에서 `password_change_required = 'N'` 자동 초기화
- `src/main/java/io/moer/booking/domain/auth/dto/LoginResponse.java` — `passwordChangeRequired` 필드 노출 (FE 라우팅용). `@Builder.Default` 로 Lombok 경고 해소
- `CLAUDE.md` — 초기 비밀번호 평문 제거, 운영자가 설정하도록 안내
- `docs/initdata.sql` — 개발 시드 명시, `password_change_required = 'Y'` 설정 및 비밀번호 변경 강제 코멘트 추가

### ✅ P3-5: 감사 로그 커버리지 점검

**점검 결과**: SuperAdminUserService(changeRole/suspend/activate/delete), UserService(updateUserStatus), AuthService(register/passwordChange/accountDelete), BusinessService(주요 액션) 등은 이미 audit log 기록. **StaffService.deleteStaff 만 누락 확인 → 추가**.

**변경 파일**:
- `src/main/java/io/moer/booking/domain/auditlog/AuditAction.java` — `STAFF_CREATED`, `STAFF_UPDATED`, `STAFF_DELETED` enum 추가
- `src/main/java/io/moer/booking/domain/staff/service/StaffService.java` — `AuditLogService` 주입, `deleteStaff(businessId, staffId, actor)` 시그니처 + 감사 로그 기록. 기존 시그니처는 `@Deprecated` 처리
- `src/main/java/io/moer/booking/domain/staff/controller/StaffController.java` — `@AuthenticationPrincipal` 추가, `requireAccessBusiness()` + actor 전달

### ✅ P3-6: WAF / DDoS 인프라 보안 가이드

**신규 파일**:
- `docs/security/infrastructure-security.md` — CloudFlare/AWS WAF 비교, DDoS 방어, TLS/HSTS, 시크릿 매니저, 모니터링, DR/백업, 적용 우선순위 정리

### ✅ P3-7: 비밀번호 정책 강화 (NIST 800-63B)

**신규 파일**:
- `src/main/java/io/moer/booking/common/util/PasswordPolicy.java`
  - 최소 10자 / 최대 128자
  - 흔한 비밀번호 블랙리스트 (`password`, `qwerty`, `admin`, `moer*` 등)
  - 단일 문자 반복 차단
  - 이메일 로컬파트 포함 차단

**변경 파일**:
- `src/main/java/io/moer/booking/domain/auth/service/AuthService.java` — `register()`, `resetPassword()`, `changePassword()` 에 `PasswordPolicy.validate()` 적용

### ✅ P3-8: CSP 정책 강화 + Permissions-Policy

**변경 파일**:
- `src/main/java/io/moer/booking/common/config/SecurityConfig.java`
  - prod CSP: `'unsafe-inline'` 완전 제거, `script-src 'self'`, `style-src 'self'`, `form-action 'self'`, `upgrade-insecure-requests` 추가
  - dev CSP: Swagger UI 호환 위해 `'unsafe-inline'` 유지
  - `Permissions-Policy` 헤더 추가: camera/microphone/usb/sensors 등 불필요 API 차단

### ✅ 빌드 검증
- `./gradlew compileJava` → **BUILD SUCCESSFUL** (47s, 캐시 사용)
- 경고 0건 (P1-2의 `LoginResponse.tokenType` Lombok 경고도 `@Builder.Default` 적용으로 해소)

---

## 6. 변경 파일 요약 (P0 + P1 통합)

```
신규 (P0):
  .env.example
  src/main/resources/application-local.yml.example
  src/main/resources/application-prod.yml.example
  src/main/java/io/moer/booking/common/config/SecretsValidator.java
  docs/security/ENV-SETUP.md
  docs/history/2026-05-12.md

신규 (P1):
  src/main/java/io/moer/booking/common/util/MaskingUtils.java
  src/main/java/io/moer/booking/common/util/SqlEscapeUtils.java
  src/main/java/io/moer/booking/common/util/FilenameUtils.java
  src/main/java/io/moer/booking/common/util/HtmlSanitizer.java
  src/main/java/io/moer/booking/common/ratelimit/RateLimiterService.java
  src/main/java/io/moer/booking/common/ratelimit/RateLimitFilter.java

신규 (P2):
  src/main/java/io/moer/booking/common/pagination/PageSizeCapFilter.java

수정 (P2):
  build.gradle.kts
  src/main/resources/application.yml
  src/main/resources/application-local.yml.example
  src/main/java/io/moer/booking/common/config/PasswordEncoderConfig.java
  src/main/java/io/moer/booking/common/config/SecurityConfig.java

신규 (P3):
  src/main/resources/db/migration/V20260512__security_enum_check_constraints.sql
  src/main/java/io/moer/booking/common/util/PasswordPolicy.java
  docs/security/infrastructure-security.md

수정 (P3):
  src/main/resources/db/schema.sql
  src/main/resources/mapper/user/UserMapper.xml
  src/main/java/io/moer/booking/common/exception/GlobalExceptionHandler.java
  src/main/java/io/moer/booking/common/config/WebConfig.java
  src/main/java/io/moer/booking/common/config/SecurityConfig.java
  src/main/java/io/moer/booking/domain/user/User.java
  src/main/java/io/moer/booking/domain/auth/dto/LoginResponse.java
  src/main/java/io/moer/booking/domain/auth/service/AuthService.java
  src/main/java/io/moer/booking/domain/auditlog/AuditAction.java
  src/main/java/io/moer/booking/domain/staff/service/StaffService.java
  src/main/java/io/moer/booking/domain/staff/controller/StaffController.java
  CLAUDE.md
  docs/initdata.sql

수정 (P0):
  .gitignore
  docker-compose.yml
  src/main/resources/application.yml
  src/main/resources/mapper/business/BusinessSettingsMapper.xml
  src/main/resources/mapper/customer/CustomerMapper.xml
  src/main/java/io/moer/booking/common/security/JwtProperties.java
  src/main/java/io/moer/booking/common/storage/LocalFileStorageService.java
  src/main/java/io/moer/booking/common/config/SecurityConfig.java
  src/main/java/io/moer/booking/common/config/SwaggerConfig.java
  src/main/java/io/moer/booking/domain/user/controller/UserController.java

수정 (P1):
  build.gradle.kts
  src/main/resources/db/schema.sql
  src/main/resources/mapper/auth/RefreshTokenMapper.xml
  src/main/resources/mapper/business/BusinessMapper.xml
  src/main/resources/mapper/customer/CustomerMapper.xml
  src/main/resources/mapper/user/UserMapper.xml
  src/main/resources/mapper/staff/StaffMapper.xml
  src/main/resources/mapper/help/HelpArticleMapper.xml
  src/main/resources/mapper/CouponMapper.xml
  src/main/java/io/moer/booking/domain/auth/RefreshToken.java
  src/main/java/io/moer/booking/domain/auth/repository/RefreshTokenRepository.java
  src/main/java/io/moer/booking/domain/auth/service/AuthService.java
  src/main/java/io/moer/booking/common/security/OAuth2AuthenticationSuccessHandler.java
  src/main/java/io/moer/booking/common/security/CustomUserDetails.java
  src/main/java/io/moer/booking/domain/user/User.java
  src/main/java/io/moer/booking/common/service/EmailService.java
  src/main/java/io/moer/booking/domain/inquiry/service/InquiryService.java
  src/main/java/io/moer/booking/domain/customer/service/CustomerService.java
  src/main/java/io/moer/booking/domain/booking/service/PublicBookingService.java
  src/main/java/io/moer/booking/domain/review/service/ReviewService.java
  src/main/java/io/moer/booking/domain/review/controller/CustomerReviewController.java
  src/main/java/io/moer/booking/domain/payment/dto/PaymentResponse.java
  src/main/java/io/moer/booking/domain/business/controller/BusinessController.java
  src/main/java/io/moer/booking/domain/dashboard/controller/DashboardController.java
  src/main/java/io/moer/booking/domain/customer/controller/CustomerController.java
  src/main/java/io/moer/booking/domain/statistics/controller/StatisticsController.java
  src/main/java/io/moer/booking/domain/notificationlog/controller/NotificationLogController.java
  src/main/java/io/moer/booking/domain/staff/controller/StaffScheduleController.java
```
